# CreditCardService (Layer 1, external) — cổng riêng
PORT=3006

# Broker để tự đăng ký (đặt BROKER_ENABLED=false để tắt)
BROKER_URL=http://localhost:8080
BROKER_ENABLED=true

# Hạn mức: charge vượt mức này sẽ bị từ chối (dùng demo luồng thẻ bị từ chối)
CREDIT_LIMIT=1000000
