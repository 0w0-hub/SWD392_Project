{
  "name": "coordinator-service",
  "version": "0.0.1",
  "description": "Coordination layer (Gomaa Ch.22 Online Shopping System) — Customer / Supplier / Billing coordinators + user-interaction endpoints, Express, service discovery via Broker",
  "private": true,
  "license": "UNLICENSED",
  "main": "src/server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
